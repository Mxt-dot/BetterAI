# DOESN'T WORK IN ITS CURRENT ITERATION

# Reason for this is that this script, although posted in the dev diary, most probably actually does not posess any functionality.
# If it does have functionality and you found out how to use it, get in contact with me.

province = {
    war = {
        active = {
            is_at_war = yes
        }
        eval_multiply = {
        	factor = 0.1
        	modifier = {
        		always = yes
        	}
        }
    }
}
